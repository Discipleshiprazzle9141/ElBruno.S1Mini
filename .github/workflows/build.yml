name: CI Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            8.0.x
            10.0.x

      - name: Restore
        run: |
          dotnet restore src/ElBruno.S1Mini/ElBruno.S1Mini.csproj -p:TargetFrameworks=net8.0
          dotnet restore src/tests/ElBruno.S1Mini.Tests/ElBruno.S1Mini.Tests.csproj
          dotnet restore src/samples/HelloS1Mini/HelloS1Mini.csproj

      - name: Build
        run: |
          dotnet build src/ElBruno.S1Mini/ElBruno.S1Mini.csproj --no-restore -p:TargetFrameworks=net8.0
          dotnet build src/tests/ElBruno.S1Mini.Tests/ElBruno.S1Mini.Tests.csproj --no-restore
          dotnet build src/samples/HelloS1Mini/HelloS1Mini.csproj --no-restore

      - name: Test
        run: dotnet test src/tests/ElBruno.S1Mini.Tests/ElBruno.S1Mini.Tests.csproj --no-build --framework net8.0 --verbosity normal --logger "trx;LogFileName=test-results.trx"

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results
          path: "**/test-results.trx"
